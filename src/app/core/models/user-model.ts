export class UserModel {
    constructor(
        name: string,
        email: string,
        password: string,
        birthDate: number,
        phone: string,
        token?: string,
        register?: string,
    ) { }
}